# dotfiles

`$HOME/dotfiles` に展開することを想定しています．

```bash
git clone https://github.com/OpenJNY/dotfiles ~/dotfiles
```

## ファイルの説明

`.foo` のような名前のドットファイルまたはフォルダは，シンボリックリンクが張られる設定ファイル

* ただし `.gitignore` はこのレポジトリを管理する為のもの

そのほかのファイルは異なる