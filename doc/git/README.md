# git

## gitignore

`./gitignore-vscode-snippet.sh` で VSCode のスニペットを作成できる．

使い方

* `./gitignore-vscode-snippet.sh TeX latex` 等を実行
* 標準出力に表示されたスニペットを VS Code に登録
    * "Configure User Snippets" > "gitignore" にコピペするだけ